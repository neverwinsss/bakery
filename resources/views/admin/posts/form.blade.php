<form method="POST" enctype="multipart/form-data" action="{{ $post ? route('admin.posts.update',$post) : route('admin.posts.store') }}" class="bg-white rounded-xl p-6 space-y-4 max-w-2xl">@csrf @if($post) @method('PUT') @endif<input name="title" value="{{ old('title',$post->title ?? '') }}" placeholder="Заголовок" class="w-full border rounded px-3 py-2"><textarea name="content" rows="10" class="w-full border rounded px-3 py-2" placeholder="Текст">{{ old('content',$post->content ?? '') }}</textarea><input type="file" name="image" class="w-full border rounded px-3 py-2"><button class="bg-amber-600 text-white px-5 py-2 rounded">Сохранить</button></form>
